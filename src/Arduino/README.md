In the folder "EIT_H-bridge" there are two files:
- `EIT_H-bridge.ino` is the actual arduino code. 
- `com.ino` is just functions for serial communication. 

The folder `PID_v1` contains the pid algoritm used. 
